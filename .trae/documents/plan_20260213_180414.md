I will create a centralized `MainLayout` component to handle the application's layout structure based on the current route, as requested.

### Plan

1.  **Modify `src/components/navigation/Sidebar.tsx`**:
    *   Remove the inline `display: none` style to allow the sidebar to be visible when rendered. We will control visibility via the parent layout or CSS classes.

2.  **Create `src/components/MainLayout.tsx`**:
    *   This will be a **Client Component**.
    *   It will accept `children` and `user` as props.
    *   It will define the layout configuration:
        *   `"/"`: Sidebar ON, Header ON, Footer ON.
        *   `"/home"`: Sidebar ON, Header ON, Footer OFF.
        *   Default: Sidebar ON, Header ON, Footer ON (fallback).
    *   It will use `usePathname` to determine the current route and apply the configuration.
    *   It will render `HeaderNavigation`, `SidebarNavigation`, and `FooterNavigation` conditionally.
    *   It will enforce a flex layout for the Sidebar and Main content.

3.  **Update `src/app/layout.tsx`**:
    *   Fetch the current user session (id and account type) using `prisma` (since `getSession` only returns limited info, I will fetch the user details needed for the nav, or pass what's available). *Correction*: To avoid complexity, I will use `getSession` and if needed, fetch the user record in `layout.tsx` to pass the full `user` object (name, username, image) to `MainLayout`, ensuring the profile/logout features work.
    *   Replace the current hardcoded layout structure with the new `<MainLayout user={user}>{children}</MainLayout>`.

4.  **Verify**:
    *   Ensure the layout behaves as expected on `/` and `/home` (simulated check).
