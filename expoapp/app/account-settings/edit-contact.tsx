import React from 'react'; import { router } from 'expo-router'; import { Pressable, StyleSheet, TextInput, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Text } from '#/components/ui/text'; import { useAuthSession } from '#/core/hooks/useAuthSession';
const C = { red: '#820000', ink: '#191413', muted: '#786E6B', line: '#EDE6E3', paper: '#FBF8F6', white: '#FFF' };
export default function EditContact() { const { session } = useAuthSession(); const [email, setEmail] = React.useState(session?.profile.email || ''); const [phone, setPhone] = React.useState(session?.profile.phone || ''); return <SafeAreaView style={s.safe}><View style={s.content}><Head title="Phone & email" /><Field label="Email address" value={email} onChangeText={setEmail} keyboardType="email-address" /><Field label="Phone number" value={phone} onChangeText={setPhone} keyboardType="phone-pad" /><Pressable style={s.button} onPress={() => router.back()}><Text style={s.buttonText}>Save changes</Text></Pressable></View></SafeAreaView>; } function Head({ title }: { title: string }) { return <View style={s.header}><Pressable onPress={() => router.back()}><Text style={s.back}>‹</Text></Pressable><Text style={s.title}>{title}</Text><View style={{ width: 28 }} /></View>; } function Field({ label, ...p }: { label: string } & React.ComponentProps<typeof TextInput>) { return <View style={s.field}><Text style={s.label}>{label}</Text><TextInput {...p} style={s.input} placeholderTextColor="#A79A95" /></View>; } const s = StyleSheet.create({ safe: { flex: 1, backgroundColor: C.paper }, content: { padding: 20 }, header: { height: 54, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 28 }, back: { fontSize: 38, color: C.ink }, title: { fontSize: 18, fontWeight: '800', color: C.ink }, field: { marginBottom: 20 }, label: { color: C.ink, fontSize: 12, fontWeight: '800', marginBottom: 8 }, input: { backgroundColor: C.white, borderColor: C.line, borderWidth: 1, borderRadius: 13, padding: 14, color: C.ink, fontSize: 14 }, button: { backgroundColor: C.red, borderRadius: 14, alignItems: 'center', padding: 15, marginTop: 10 }, buttonText: { color: C.white, fontWeight: '800' } });
